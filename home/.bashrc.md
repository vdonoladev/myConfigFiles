## 🛠️ **Funções de Gerenciamento de Arquivos**

### `mkcd`

Cria um diretório e entra nele automaticamente

```bash
mkcd meu_projeto
# Cria a pasta "meu_projeto" e já entra nela
```

### `extract`

Extrai qualquer tipo de arquivo compactado

```bash
extract arquivo.zip
extract documento.tar.gz
extract pacote.rar
# Suporta: .zip, .tar.gz, .rar, .7z, .bz2, .deb, .tar.xz, etc
```

### `backup`

Cria backup de um arquivo com timestamp

```bash
backup config.php
# Cria: config.php.2024-11-25.bak
```

---

## 🔍 **Funções de Busca e Informação**

### `search`

Busca texto em arquivos recursivamente

```bash
search "função principal"
search "TODO"
# Busca em todos os arquivos do diretório atual e subdiretórios
```

### `topcommands`

Mostra os 10 comandos que você mais usa

```bash
topcommands
# Exemplo de saída:
# 1  150 15% git
# 2  100 10% cd
# 3   80  8% ls
```

### `dirsize`

Mostra o tamanho de cada pasta no diretório atual

```bash
dirsize
# Exemplo:
# 2.5G  node_modules/
# 500M  vendor/
# 100M  public/
```

### `bigfiles`

Encontra arquivos grandes (padrão: maiores que 100MB)

```bash
bigfiles
bigfiles 50    # Encontra arquivos maiores que 50MB
bigfiles 500   # Encontra arquivos maiores que 500MB
```

---

## 💻 **Funções de Sistema**

### `sysinfo`

Mostra informações do sistema resumidas

```bash
sysinfo
# Mostra: OS, Kernel, Hostname, Uptime, Memória, Disco
```

### `memtop`

Mostra os 10 processos que mais consomem memória

```bash
memtop
```

### `cputop`

Mostra os 10 processos que mais consomem CPU

```bash
cputop
```

### `ports`

Mostra portas abertas e processos associados

```bash
ports
# Lista todas as portas em LISTEN e qual programa está usando
```

---

## 🌐 **Funções de Desenvolvimento**

### `serve`

Cria um servidor HTTP local (padrão: porta 8000)

```bash
serve           # Abre servidor na porta 8000
serve 3000      # Abre servidor na porta 3000
# Acesse: http://localhost:8000
```

---

## 📋 **Resumo Rápido - Todas as Funções**

| Função        | O que faz             | Exemplo               |
| ------------- | --------------------- | --------------------- |
| `mkcd`        | Cria e entra em pasta | `mkcd novo_projeto`   |
| `extract`     | Extrai arquivos       | `extract arquivo.zip` |
| `backup`      | Backup com timestamp  | `backup config.php`   |
| `search`      | Busca em arquivos     | `search "TODO"`       |
| `topcommands` | Comandos mais usados  | `topcommands`         |
| `dirsize`     | Tamanho das pastas    | `dirsize`             |
| `bigfiles`    | Arquivos grandes      | `bigfiles 100`        |
| `sysinfo`     | Info do sistema       | `sysinfo`             |
| `memtop`      | Top memória           | `memtop`              |
| `cputop`      | Top CPU               | `cputop`              |
| `ports`       | Portas abertas        | `ports`               |
| `serve`       | Servidor HTTP         | `serve 8000`          |

---

## 🎯 **Funções Mais Úteis no Dia a Dia**

```bash
# 1. Criar projeto novo
mkcd meu-site && code .

# 2. Extrair arquivo baixado
extract ~/Downloads/projeto.zip

# 3. Buscar TODOs no código
search "TODO"

# 4. Ver o que está ocupando espaço
dirsize

# 5. Testar site local
serve 3000

# 6. Backup antes de editar
backup .env

# 7. Ver processos pesados
memtop
```
